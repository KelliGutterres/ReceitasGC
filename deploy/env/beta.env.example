# Copie para deploy/env/beta.env na VM (NÃO commitar senhas reais).
# cp deploy/env/beta.env.example deploy/env/beta.env

NODE_ENV=production
HOST=0.0.0.0
PORT=3000
APP_PORT=3001

PGHOST=127.0.0.1
PGPORT=5432
PGUSER=receitas_beta
PGPASSWORD=troque-esta-senha-beta
PGDATABASE=receitas_beta_db

SESSION_SECRET=receitas-gc-dev-secret

# SendGrid (opcional)
SENDGRID_API_KEY=
SENDGRID_FROM=Receitas GC Beta <email-verificado@seudominio.com>
