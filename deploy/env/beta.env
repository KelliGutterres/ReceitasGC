# Ambiente Beta — valores fixos para deploy na VM (trabalho acadêmico).
# Usado diretamente pelo docker compose; não é necessário editar na apresentação.

NODE_ENV=production
HOST=0.0.0.0
PORT=3000
APP_PORT=3001

PGHOST=127.0.0.1
PGPORT=5432
PGUSER=receitas_beta
PGPASSWORD=ReceitasGC_beta_2026
PGDATABASE=receitas_beta_db

# Opcional no código — se omitido, src/server.js usa "receitas-gc-dev-secret"
SESSION_SECRET=receitas-gc-dev-secret

# SendGrid (opcional — app funciona sem)
SENDGRID_API_KEY=
SENDGRID_FROM=Receitas GC Beta <email-verificado@seudominio.com>
