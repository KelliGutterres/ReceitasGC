# Copie para deploy/env/prod.env na VM (NÃO commitar senhas reais).
# cp deploy/env/prod.env.example deploy/env/prod.env

NODE_ENV=production
HOST=0.0.0.0
PORT=3000
APP_PORT=3000

PGHOST=127.0.0.1
PGPORT=5432
PGUSER=receitas_prod
PGPASSWORD=troque-esta-senha-prod
PGDATABASE=receitas_prod_db

SESSION_SECRET=receitas-gc-dev-secret

# SendGrid (opcional)
SENDGRID_API_KEY=
SENDGRID_FROM=Receitas GC <email-verificado@seudominio.com>
